/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    // Los dominios de donde se permitirán extraer imagenes
    domains: ['raw.githubusercontent.com']
  }
}
